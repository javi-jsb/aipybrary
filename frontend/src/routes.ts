/** The route an authenticated user lands on after login / from the app root. */
export const DEFAULT_AUTHENTICATED_ROUTE = "/books";

/** The login route — where unauthenticated access is redirected. */
export const LOGIN_ROUTE = "/login";
