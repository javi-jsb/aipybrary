# Base URL of the aipybrary backend API.
# The frontend calls the API directly cross-origin; CORS must be enabled on the backend.
# Defaults to http://localhost:8000 when unset.
VITE_API_BASE_URL=http://localhost:8000
